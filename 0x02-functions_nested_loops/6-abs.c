#include "main.h"

/**
 * _abs - writes the absolute value of an integer
 * @n: contains the integer to check
 * Return: Always 0 (Successful code)
 */

int _abs(int n)
{
	return (n >= 0 ? n : n * -1);
}
